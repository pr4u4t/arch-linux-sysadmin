server.username		= "${USER}"
server.groupname	= "${GROUP}"
server.document-root	= "${HOME}"
server.errorlog		= "${ERRLOG}/error.log"
dir-listing.activate	= "${DIR_LIST}"
server.bind		= "${SERVER_ADDRESS}" 
server.port 		= ${PORT}
index-file.names	= ( "index.html" )

